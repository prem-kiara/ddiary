# Dependencies
node_modules/
functions/node_modules/

# Build output
dist/
dist-check*/
.firebase/

# Environment variables — NEVER commit these
.env
.env.local
.env.*.local

# Firebase
# (.firebaserc IS committed — it pins the project for `firebase deploy`)

# FUSE filesystem temp files (left over when editing inside a network mount)
.fuse_hidden*

# Service account key — NEVER commit this
scripts/serviceAccountKey.json
sa.json
firebase-debug.log
firestore-debug.log
storage-debug.log
ui-debug.log

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo
.env
